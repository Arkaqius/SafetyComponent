# Development Guide - Backend

## Prerequisites
- Python (version not specified in repo)

## Local Development
- No formal setup instructions found. Core modules are under `backend/shared/`.

## Tests
- Pytest-style tests are under `backend/tests/`.
- Suggested command: `python -m pytest backend/tests` (verify locally).
